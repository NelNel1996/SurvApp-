name: survapp
description: Application de gestion des surveillants de devoirs
publish_to: 'none'
version: 1.0.0+1

environment:
  flutter: "3.7.12"
  xcode: latest

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
